# Server
host: 0.0.0.0
port: 80

# Security
jwt_secret: 9928b1157091
users:
    admin: admin

# Docker Hosts
docker_hosts:
    default: local
    hosts:
        local:
            driver: socket
            endpoint: /var/run/docker.sock
            display_name: LocalDocker
            mount_points:
                docker:
                    path: /opt/docker
                    read_only: false
